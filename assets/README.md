# Profile assets

`arthoqox-hero-v2.png` is the personal identity artwork created specifically for the Arthoqox profile. Keep this file in the repository because the profile README references it with a relative path.

`hiddenode-hero.png` is retained as an earlier concept asset but is no longer used by the profile README.
